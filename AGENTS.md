# Smoke repository rules

- Use Python standard library only.
- Keep changes limited to the requested utility and its tests.
- Run `python3 -m unittest discover -s tests -v` before finishing a task.
- Never add credentials, tokens, private keys, or cloud configuration files.
