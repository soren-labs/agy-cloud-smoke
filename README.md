# agy-cloud smoke repository

Private target repository for the agy-cloud first-version smoke flow. Agents may create `agy/*` branches and pull requests here. It contains only disposable utility code and tests; never add cloud credentials or production data.
